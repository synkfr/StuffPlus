name: Build and Package Stuff+

on:
  push:
    branches:
      - master
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - master
      - main

permissions:
  contents: write

jobs:
  build:
    name: Build & Deploy Shaded JAR
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant Execute Permission to Gradlew
        run: chmod +x gradlew || true

      - name: Build with Gradle
        run: ./gradlew build

      - name: Upload Shaded JAR Artifact
        uses: actions/upload-artifact@v4
        with:
          name: StuffPlus-Plugin
          path: build/libs/*.jar
          if-no-files-found: error
          retention-days: 7

      # 1. Dev Build (Pre-Release) Deployment
      - name: Deploy Dev Pre-Release Build
        if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          echo "Deploying rolling dev pre-release..."
          
          # Delete any previous rolling dev-latest release to maintain a clean rolling release
          git tag -d dev-latest || true
          gh release delete dev-latest --yes || true
          git push --delete origin dev-latest || true
          
          # Create rolling dev-latest pre-release pointing explicitly to the pushed commit
          gh release create "dev-latest" build/libs/*.jar \
            --prerelease \
            --title "Dev Build (Latest)" \
            --target "${{ github.sha }}" \
            --notes "Latest development pre-release build from branch master. Commit: $SHORT_SHA (Run #${{ github.run_number }})."

      # 2. Production Release Deployment
      - name: Deploy Production Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ github.ref_name }}"
          echo "Deploying production release: $TAG_NAME"
          
          gh release create "$TAG_NAME" build/libs/*.jar \
            --title "Release $TAG_NAME" \
            --notes "Official production release of Stuff+ ($TAG_NAME)."
